allprojects {
    apply plugin: 'base'

    group = 'com.enonic.osgi.bundles'

    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'wrap'

    wrap {
        repo = 'http://repo.enonic.com/artifactory/public'
    }
}
