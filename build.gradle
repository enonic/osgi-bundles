allprojects {
    apply plugin: 'base'

    group = 'com.enonic.osgi.bundles'

    repositories {
        mavenCentral()
    }
}

subprojects {

    apply from: "${rootProject.rootDir}/gradle/wrap.gradle"

}
