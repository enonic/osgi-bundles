version = '9.5.1'

ext {
    lib = 'net.sf.saxon:Saxon-HE:9.5.1-4'
}

dependencies {
    wrap "${lib}@jar"
    wrapSources "${lib}:sources@jar"
}

jar {
    manifest {
        name = 'Saxon HE Library'
        instruction 'Import-Package', '*;resolution:=optional'
    }
}
